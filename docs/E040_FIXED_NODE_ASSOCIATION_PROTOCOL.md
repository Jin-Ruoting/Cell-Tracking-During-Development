# E040：固定节点上的独立关联检验

在首次 E040 图像关联输出和标签评分之前固定本方案。假设是：v12 的独立
多尺度外观特征和跨帧注意力，可纠正 E029 中部分两细胞身份交换；保持检测
坐标、节点数及所有节点度数不变，可将比较集中到关联信息。

这是本项目设计的组件实验，并非作者完整 v12 跟踪流程的复现，也未得到
优于 E029 的质量证据。v12 自行检测出的越界坐标不进入此实验；新方案直接
采样 E029 的原坐标，不移动、裁剪、添加或删除节点。

## 固定特征与数据

- 来源：[v12 作者演示](https://www.kaggle.com/code/hengck23/end2end-cell-linker-raw-edge-ja-0-9-no-ilp)。
  权重保持 SHA256
  `ecd8869de9cf405c1a93a563b4810faad5fd2378f57341e18c4e6b5a87201552`，
  模型源码保持 `586ac08e4429fa0ca0db0c06afdf6d7ef77d459c0260bbf286bf47169d484082`。
  继续完整文件/CRC校验、受限反序列化和严格参数加载，不重训。
- 按作者的 `(1,4,4)` 步长、图像属性 `0.001/0.999` 分位数和下限零截断预处理。
  运行 UNet 特征主干，在 E029 坐标除以 `(1,4,4)` 后的位置采样 `[d0,d1,e2]`，
  使用原作者 `grid_sample` 约定、位置编码和连接 Transformer。检测/坐标细化
  头不运行；保留完整帧节点参与注意力及候选排名，禁止按已有边裁剪候选池。
- 每帧所有固定节点都参与；超过已审查的 1024 节点范围时停止并报告，不为
  适配内存丢点。没有节点的帧应作为显式无连接情况处理，不合成节点。
- 技术检查复用 S210 独立云端控制，CSV SHA256
  `4cae8278063f1ec44932750f9585b45805efb944540ccea670e2d3fa5687d68f`；
  验证其源码、重复推理回执与原字节。它不是在新任务里重新推理的控制。
  两部技术影片为 `44b6_eb2880fc`、`6bba_969618f6`。
- 完整比较仍使用既有 64 部集合，清单 SHA256
  `276c09d16cddaf2e865896ce147161a7beb5a62142bf47c1f1bd7648f7643e7f`。
  复用完成并审查过的 S213 云端 E029 控制，核验冻结哈希、两部锚点及来源后
  才能开始完整 E040。E039 桥接候选不作为控制，也不与 E040 组合。
- 预测只读训练图像与固定控制 CSV；全部候选完成并冻结后再读取标签评分。
  不调用作者的带标签数据加载器或评价循环。没有隐藏测试数据调参。

## 单次交换规则

原边为 `a→b`、`c→d`，仅考虑两个源都只有一条出边的情况。只有以下条件
全部成立，才同时替换为 `a→d`、`c→b`：

1. 在完整相邻帧矩阵中，`a` 唯一最优目标为 `d`、`c` 唯一最优目标为 `b`，
   且 `d` 唯一最优来源为 `a`、`b` 唯一最优来源为 `c`。相等最高值全部弃用。
2. 两条新边 logits 均大于固定空连接 logit `0`；该基准沿用作者演示。
3. 新边 logit 之和减去旧边之和至少为 `ln(9)`。这是评分前固定的强偏好
   门槛，不是校准后的正确概率，也不是根据技术影片选择出的参数。
4. 两条新边的物理距离均不超过 `5.5 um`，沿用 E029 固定紧连接距离。

唯一互选的两环互不共享端点，单次同时应用；不做迭代、不处理更长环、
不在拒绝后寻找次优替代。总边数、每个节点入度和出度、原有分裂边必须
逐项不变；缺失端点、重复边、非法坐标或不完整 logits 直接失败。

## 检查与资源

1. 数据无关语义测试检查成功交换、非互选/并列/空连接/弱偏好/过远拒绝、
   分裂保护、完整候选池、长环不改写、节点 ID 与矩阵次序无关、非法输入失败。
2. 私有离线 Kaggle CPU 先检查两部影片各第 0、1 帧，FP32、4 线程；子进程
   20 分钟、Notebook 25 分钟上限。报告节点数、logit 范围、交换计数/原因、
   原图不变性、时间和内存，不计算标签分数。其是否出现交换不选择参数。
3. 后续使用固定 T4/FP32、关闭 TF32，先完成两部全 100 帧技术流程；再按
   实测预算安排一次完整 64 部比较，最多 8 小时，不并发复制相同任务。
   CPU 四帧检查不能代替 GPU 全流程。启动前刷新免费额度，不购买资源。
4. 完整图审查应证明节点逐字段相同、度数和分裂边不变、真正改变的普通边数、
   完整 100 帧覆盖、控制/候选哈希在评分期间不变。复用控制须明确记录来源。

## 完整判定

只以 64 部完整官方比较判定：总分增益至少 `0.001`、调整后边分数不下降、
两类胚胎和交替两半增益均为正、受影响影片胜多于负且中位增益为正。还需
验证分裂 TP/FP/FN 与原图完全一致，且确有边发生变化。失败即拒绝，不改
距离、偏好门槛或互选规则重新筛选。

通过后额外在 58 部可见作者选片排除集合上检查全体与两胚胎增益均为正。
排除 `44b6_12dfb391`、`44b6_267148e4`、`44b6_2a2eff9f`、`44b6_341df25f`、
`44b6_949adeb1`、`6bba_09961292`。训练影片重叠仍未知，这只是开发敏感性
审查，不是独立验证。正式提交还需完整运行输出审计，并以实际公榜回执检验
是否改善 E029 的 `0.947` 和当前名次。技术运行本身不能晋级。

## 当前实现范围

`kaggle/v12_edge_swap.py` 实现冻结的交换机制；
`kaggle/check_v12_fixed_nodes.py` 实现私有 CPU 四帧检查。
`kaggle/run_v12_association.py` 与 `kaggle/build_v12_association.py` 已实现完整
影片的双 T4 分片、官方配对评分和 58 部排除审查，实际 GPU 检查待完成。
打包完整 64 部必须同时提供同协议的 GPU 技术回执和完成的 S213 控制回执。
所有源码本地提交并推送后才打包；日志/原控制/模型保持在被忽略的运行位置，
不进入公开 Git。

## CPU 技术结果

私有 Kaggle CPU 检查已完成，源码 `8955f96b458e635e36664fab1039b086896d75bb`。
独立控制 CSV 的字节和来源通过检查，10 项交换语义测试通过。两部影片首对
帧分别包含 365/383 与 180/183 个固定节点，完整 logit 矩阵为对应的矩形，
全部有限；原控制节点和边对象未被修改。两个帧对均无互选两环，因此交换 0
条边，没有官方评分或质量结论。总耗时 36.28 秒，峰值 CPU 内存约 1.44 GiB。

GPU 适配增加了 4 项完整图读写测试，验证空帧索引、原节点字段和分裂边保护。
后续沿用原交换条件，不以四帧计数选择参数。GPU 使用 FP32 并关闭 TF32；
与 CPU 不主张逐位一致，正式比较始终使用相同的冻结控制图。
